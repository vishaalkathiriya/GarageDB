This sample project has StaffMembers and StaffRoles outside the built-in user management. This is for when staff members will not have their own logins.

Navigation properties are used to create a many to many relationship between the StaffMember and the StaffRole models.